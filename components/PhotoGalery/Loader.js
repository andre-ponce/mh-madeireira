export function Loader() {
  return (
    <div
      style={{
        width: '100%',
        height: '400px',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontSize: "40px",
        color: "#ccc"
      }}
    >
      <i className="fa fa-spin fa-spinner"></i>
    </div>
  );
}
